%br
.row
  %center
    %img{:src => "#{asset_path("logo.png")}", :style => "width: 200px;"}
%br
.row
  %center
    %h4.text-info Customer Verification
    %br
    =simple_form_for(@customer_verification) do |f|
      = f.error_notification
      .form-inputs
        = f.input :cell_number, :label => "Cell Number"
      .form-actions
        = f.button :submit, "Verify", :class => "btn btn-success btn-block"
.row
  %center
    %img{:src => "#{asset_path("veridot.png")}", :style => "width: 80px;"}
